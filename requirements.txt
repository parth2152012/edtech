# PDF processing
PyPDF2>=3.0.0
PyMuPDF>=1.22.0  # for fitz module

# Document processing
docx2txt>=0.8.0

# Image processing
Pillow>=9.0.0

# Audio and Speech processing
gTTS>=2.2.3
SpeechRecognition>=3.8.1
pydub>=0.25.1
audio_recorder_streamlit==0.0.10  # fixed: latest available version

# Translation
googletrans==4.0.0-rc1

# Core dependencies
streamlit==1.30.0
streamlit-extras>=0.3.0  # for enhanced UI components
google-generativeai==0.3.1
python-dotenv==1.0.0

# Streamlit rich logging compatibility
rich<14,>=10.14.0  # fixed: rich 14.x not compatible with streamlit 1.30.0

# Standard libraries (built-in — do not install via pip):
# re, os, io, datetime, time, tempfile, base64, hashlib, logging
